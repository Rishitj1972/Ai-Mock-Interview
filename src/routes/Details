1. Typical route categories in a project
🔹 Public routes (no login required)

/ → HomePage

/about → AboutPage

/contact → ContactPage

👉 These pages are wrapped with PublicLayout (header + footer visible).

🔹 Auth routes (for authentication flow)

/sign-in → SignInPage

/sign-up → SignUpPage

/forgot-password → ForgotPasswordPage

👉 These are wrapped in AuthLayout (usually minimal, no main navbar/footer, just a centered card).

🔹 Protected routes (login required)

/dashboard → DashboardPage

/profile → ProfilePage

/settings → SettingsPage

👉 These use ProtectedLayout (often has a sidebar, different navigation).

If the user is not signed in, they get redirected to /sign-in.